//square numbers

import static java.lang.Math.*;

public class Square {    
    public static boolean isSquare(int n) { 
          if(floor(sqrt(n)) == sqrt(n)) { 
            return true;
          } else {
            return false;
          }
}
}
